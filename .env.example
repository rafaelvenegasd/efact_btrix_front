# Backend base URL — override with actual NestJS URL
VITE_API_BASE_URL=http://localhost:4000

# Polling interval while invoice is in pending state (ms)
VITE_POLLING_INTERVAL_MS=5000

# Dev-only simulated token — used when no ?token= is present in the URL.
# Remove or leave empty once real Bitrix24 auth is wired up.
VITE_DEV_TOKEN=
